{
  "name": "live-audience-app",
  "version": "1.0.0",
  "description": "Live audience engagement app similar to Slido",
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix server",
    "build": "NPM_CONFIG_PRODUCTION=false npm install --prefix client && npm run --prefix client build && npm install --prefix server",
    "start": "node server/index.js",
    "dev:server": "node server/node_modules/nodemon/bin/nodemon.js server/index.js",
    "dev:client": "cd client && node node_modules/vite/bin/vite.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
