[phases.install]
cmds = [
  "npm install --prefix server"
]

[phases.build]
cmds = ["npm install --prefix client --include=dev && npm run --prefix client build"]

[start]
cmd = "node server/index.js"

[variables]
NODE_VERSION = "20"
